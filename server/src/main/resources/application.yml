server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://database-1.cgxb4bo4zmgn.ap-northeast-2.rds.amazonaws.com/FreeDB?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: admin
    password: q1w2e3r4
  jpa:
    hibernate:
      ddl-auto: update

  decorator:
    datasource:
      p6spy:
        enable-logging: true

